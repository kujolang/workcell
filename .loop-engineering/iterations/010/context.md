# Iteration 010 context

- Objective: continue the next review backlog with the repository-side egress/proxy enforcement contract.
- Scope: validate network egress declarations, preserve the effective policy in inspection and receipts, add warnings for legacy unmanaged access, and add a runnable managed-egress example.
- External boundary: the host firewall/proxy and Linux rootless deployment still require deployment evidence.
