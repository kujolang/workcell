diff --git a/src/domain/definition.kujo b/src/domain/definition.kujo
index 31774ec..c81141c 100644
--- a/src/domain/definition.kujo
+++ b/src/domain/definition.kujo
@@ -353,7 +353,12 @@ while digit_i < len(parts) {
     }
 digit_i = digit_i + 1
 }
-numeric := parse_int(part)
+mut numeric := 0
+try {
+    numeric = parse_int(part)
+} except err {
+return {"ok": false, "error": "workspace.run_as contains an invalid uid/gid number"}
+}
 if numeric <= 0||numeric > 65534 {
@@ -412,7 +417,12 @@ while i < len(amount_text) {
     }
 i = i + 1
 }
-amount := parse_int(amount_text)
+mut amount := 0
+try {
+    amount = parse_int(amount_text)
+} except err {
+return false
+}
 if amount <= 0 {
diff --git a/src/util.kujo b/src/util.kujo
index a221cd4..f27fa69 100644
--- a/src/util.kujo
+++ b/src/util.kujo
@@ -381,7 +381,12 @@ while i < len(value) {
     }
 i = i + 1
 }
-number := parse_int(value)
+mut number := 0
+try {
+    number = parse_int(value)
+} except err {
+return false
+}
 return number > 0&&number <= 65534
diff --git a/tests/workcell_test.kujo b/tests/workcell_test.kujo
index 57442fb..2f86239 100644
--- a/tests/workcell_test.kujo
+++ b/tests/workcell_test.kujo
@@ -281,8 +281,10 @@ expect("workspace scan depth limit is bounded", with_workspace_scan(definition,
 expect("fixed non-root workspace identity is accepted", validate_definition(with_run_as(definition, "65532:65532"))["ok"])
 expect("rootless workspace identity is accepted", validate_definition(with_run_as(definition, "rootless"))["ok"])
 expect("root workspace identity is rejected", validate_definition(with_run_as(definition, "0:0"))["ok"] == false)
+expect("overflow workspace identity rejected without runtime crash", validate_definition(with_run_as(definition, "999999999999999999999:1"))["ok"] == false)
 expect("CPU limit is bounded", validate_definition(with_resource(definition, "cpus", 65))["ok"] == false)
 expect("memory limit is bounded", validate_definition(with_resource(definition, "memory", "128g"))["ok"] == false)
+expect("overflow memory limit rejected without runtime crash", validate_definition(with_resource(definition, "memory", "999999999999999999999g"))["ok"] == false)
 expect("PID limit is bounded", validate_definition(with_resource(definition, "pids", 70000))["ok"] == false)
