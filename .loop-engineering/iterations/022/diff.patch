diff --git a/src/report/report.kujo b/src/report/report.kujo
index 3d509a2..e76c451 100644
--- a/src/report/report.kujo
+++ b/src/report/report.kujo
@@ -16,16 +16,30 @@ func metric(text, label) {
     mut i := 0
     while i < len(lines) {
         line := trim(lines[i])
-        if starts_with(line, label) {return parse_int(trim(substring(line, len(label), len(line))))}
-        i = i + 1
-    }
+        if starts_with(line, label) {
+            mut value := 0
+            try {
+                value = parse_int(trim(substring(line, len(label), len(line))))
+            } except err {
+            return 0
+        }
+    return value
+}
+i = i + 1
+}
 return 0
 }
@@
-    return parse_int(trim(loaded["text"]))
+    mut value := 0
+    try {
+        value = parse_int(trim(loaded["text"]))
+    } except err {
+    return fallback
+}
+return value
 }
diff --git a/tests/report_contract.sh b/tests/report_contract.sh
index c120e75..f1747ef 100755
--- a/tests/report_contract.sh
+++ b/tests/report_contract.sh
@@ -13,4 +13,8 @@ for name in policy-artifact-verification workspace-change-report performance str
 done
 
 "$KUJO" run "$ROOT/src/report/report.kujo" -- --dir "$TMP_DIR" | jq -e '.schema_version == "workcell-report/v1" and .ok == true and .totals.passed == 8 and .totals.failed == 0 and .totals.elapsed_ms == 500 and .deployment_gates.docker_integration == "skipped"' >/dev/null
+printf 'Passed: 999999999999999999999\nFailed: 0\n' > "$TMP_DIR/policy-artifact-verification.log"
+printf '999999999999999999999\n' > "$TMP_DIR/policy-artifact-verification.exit"
+malformed_report="$($KUJO run "$ROOT/src/report/report.kujo" -- --dir "$TMP_DIR")"
+printf '%s\n' "$malformed_report" | jq -e '.schema_version == "workcell-report/v1" and .ok == false and .suites[0].passed == 0 and .suites[0].exit_code == 127' >/dev/null
 echo "Report contract passed"
