# Action

- Added a structured metric parser carrying both value and validity.
- Made suite success require valid `Passed:` and `Failed:` metrics in addition to a zero exit code and zero failures.
- Added a metric-only malformed-evidence regression alongside the malformed exit regression.
- Formatted the modified Kujo source and reran report and quality gates.
