# Action

- Added one shared `validate_definition` guard to `build_policy` and `spawn_options`.
- Made `inspect_policy` propagate the structured policy validation failure before reading definition fields.
- Added three regressions for incomplete definitions across the exported policy APIs.
- Updated build, security, hardening, backlog, development, tooling, and loop summary evidence to 111/11/5 offline assertions and 127 release assertions.
