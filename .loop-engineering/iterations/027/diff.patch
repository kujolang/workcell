diff --git a/src/domain/definition.kujo b/src/domain/definition.kujo
@@
+    definition_check := validate_definition(definition)
+    if definition_check["ok"] == false { return definition_check }
diff --git a/src/runtime/docker.kujo b/src/runtime/docker.kujo
@@
+    definition_check := validate_definition(definition)
+    if definition_check["ok"] == false { return definition_check }
+    if type(policy) != "dict" || incomplete policy fields { return structured error }
diff --git a/src/receipts/receipt.kujo b/src/receipts/receipt.kujo
@@
+    definition_check := validate_definition(definition)
+    if definition_check["ok"] == false { return definition_check }
diff --git a/tests/workcell_test.kujo b/tests/workcell_test.kujo
@@
+expect("partial labels definition is rejected without runtime crash", partial_labels["ok"] == false)
+expect("partial definition summary is rejected without runtime crash", partial_summary["ok"] == false)
+expect("partial daemon security definition is rejected without runtime crash", partial_daemon_security["ok"] == false)
+expect("partial verification definition is rejected without runtime crash", partial_verification["ok"] == false)
+expect("partial integrations definition is rejected without runtime crash", partial_integrations["ok"] == false)
+expect("partial image build definition is rejected without runtime crash", partial_build["ok"] == false)
+expect("partial image ensure definition is rejected without runtime crash", partial_ensure["ok"] == false)
+expect("partial receipt definition is rejected without runtime crash", partial_receipt["ok"] == false)
+expect("partial container policy is rejected without runtime crash", partial_container["ok"] == false)
