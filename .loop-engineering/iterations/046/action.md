# Action

- Made `save_json` return `false` when its parent directory cannot be created or atomic persistence raises an error.
- Added a regression for a regular-file parent path.
- Updated release evidence to 177 offline Workcell assertions plus 18 workspace and 5 stress assertions, with 200 counted release assertions.
